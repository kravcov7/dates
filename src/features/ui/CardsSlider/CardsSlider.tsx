import styles from "./CardsSlider.module.css";
import Image from "next/image";

export const CardsSlider = () => {
  //   const { children } = props;

  return (
    <div className={styles.slider}>
      <div className={styles.slider2}>wefw</div>
    </div>
  );
};
